server:
  port: 8082
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/rrs-waitque
      client:
        registration:
          company-service:
            client-id: company-service
            client-secret: ${COMPANY_SERVICE_CLIENT_SECRET}
            authorization-grant-type: client_credentials
            provider: keycloak
        provider:
          keycloak:
            token-uri: http://keycloak:8080/realms/rrs-waitque/protocol/openid-connect/token
  datasource:
    url: jdbc:postgresql://localhost:5432/waitque
    username: waitque_api
    password: ${WAITQUE_DB_PASSWORD:}
    jpa:
      hibernate:
        ddl-auto: validate # or validate / create-drop (dev only)
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          default_schema: company

microservices:
  self:
    base-url: http://localhost:8082
  customer-service:
    base-url: http://customer-service:8083

logging:
  level:
    org.springframework.security: INFO
    com.rrsgroup: DEBUG
---
spring:
  profiles:
    active: dev
---
spring:
  config:
    activate:
      on-profile: dev

microservices:
  self:
    base-url: http://localhost:8082
  customer-service:
    base-url: http://localhost:8083
---
spring:
  config:
    activate:
      on-profile: k8s

  datasource:
    url: jdbc:postgresql://postgres:5432/waitque
    username: waitque_api
    password: ${WAITQUE_DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: company

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/rrs-waitque
      client:
        registration:
          company-service:
            client-id: company-service
            client-secret: ${COMPANY_SERVICE_CLIENT_SECRET}
            authorization-grant-type: client_credentials
            provider: keycloak
        provider:
          keycloak:
            token-uri: http://keycloak:8080/realms/rrs-waitque/protocol/openid-connect/token

microservices:
  self:
    base-url: http://localhost:8082
  customer-service:
    base-url: http://customer-service:8080
---
spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: jdbc:postgresql://postgres:5432/waitque

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_URL}/realms/rrs-waitque
      client:
        provider:
          keycloak:
            token-uri: ${KEYCLOAK_URL}/realms/rrs-waitque/protocol/openid-connect/token

microservices:
  self:
    base-url: http://waitque.com
---
spring:
  config:
    activate:
      on-profile: ecs-staging

  datasource:
    url: jdbc:postgresql://${DB_HOST:}:5432/waitque?currentSchema=waitque

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ALB_URL}/realms/rrs-waitque
      client:
        provider:
          keycloak:
            token-uri: ${KEYCLOAK_ALB_URL}/realms/rrs-waitque/protocol/openid-connect/token

microservices:
  self:
    base-url: ${ALB_URL}/2
  customer-service:
    base-url: ${ALB_URL}/3